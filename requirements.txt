flask>=3.0.3
duckdb>=1.0.0
gunicorn
pandas>=2.0.0
# numpy 2.4.0 raised the x86-64 wheel baseline to x86-64-v2 (SSE4.2 + POPCNT).
# The production server's CPU does not expose those flags, so importing pandas
# fails at startup. Drop this pin once the server CPU/VM model supports v2.
numpy<2.4
pyarrow>=15.0.0
requests>=2.28.0
openpyxl>=3.0.0
